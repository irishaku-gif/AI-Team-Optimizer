{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "preinstall": "node ./backend/scripts/ensure-pnpm.mjs",
    "dev": "vercel dev",
    "db:migrate": "node ./backend/scripts/migrate-vercel-api.mjs",
    "codegen": "pnpm --filter @workspace/api-spec run codegen",
    "build": "pnpm run db:migrate && pnpm run typecheck && pnpm --filter @workspace/team-optimizer run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm --filter @workspace/team-optimizer --filter @workspace/scripts --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}