# Project Docs

- `replit.md` - current architecture notes for the production-only Vercel workspace.
- `mockup-sandbox/` - archived mockup sandbox. It is not a supported runtime mode and must not affect the production Vercel path.

Keep docs in sync with changes to setup, deploy, API contracts, database migrations, and production runtime behavior.