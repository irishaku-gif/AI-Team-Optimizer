# Backend Support

Runtime API files stay in the repository-level `api/` directory because Vercel discovers serverless functions from that platform path.

This directory contains backend support tooling, including migration scripts used by the production build.